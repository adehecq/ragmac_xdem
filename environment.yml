name: ragmac_xdem
channels:
  - conda-forge
dependencies:
  - numpy
  - scipy
  - matplotlib
  - toml
  - pandas
  - xdem
  - opencv
  - pip
  # dev requirements
  - pytest
  - pip:
    - "git+https://github.com/GlacioHack/GeoUtils.git"
    - "git+https://github.com/GlacioHack/xdem.git" 
